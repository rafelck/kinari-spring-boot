spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kinari_db
    username: postgres
    password: rafelino
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
